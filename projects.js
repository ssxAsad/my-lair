// Navigate to the specific project
function openProject(url) {
    window.location.href = url;
}

// Placeholder for empty slots
function comingSoon() {
    alert("This project slot is currently empty. Build something new!");
}