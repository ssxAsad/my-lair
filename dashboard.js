// Function to handle Realm entrance alerts
function enterRealm(realmName) { 
    alert("Entering " + realmName.toUpperCase()); 
}

// Note: The Portfolio card interaction is handled directly in the HTML via window.location.href
// to ensure immediate redirection without needing extra JS logic here.